# Environment Variables Template
# Copy this file to .env.local for local development
# Vite exposes variables prefixed with VITE_ to your client-side code

# Application Configuration
VITE_APP_TITLE=Idle Quest

# API Configuration (if you add a backend in the future)
# VITE_API_URL=http://localhost:3000/api
# VITE_API_TIMEOUT=5000

# Feature Flags
# VITE_ENABLE_DEBUG_MODE=false
# VITE_ENABLE_ANALYTICS=false

# Game Configuration (examples)
# VITE_MAX_LEVEL=100
# VITE_STARTING_GOLD=0

# External Services (if needed in the future)
# VITE_ANALYTICS_ID=
# VITE_SENTRY_DSN=

# NOTE: Never commit sensitive keys or secrets to version control!
# Use .env.local for local development secrets
