# 🚀 Environment Variables Quick Reference

## Quick Start
```bash
# 1. Files are already created!
# 2. Edit .env to customize
# 3. Restart dev server
npm run dev
```

## Using in Code
```javascript
// Import the config module
import CONFIG from './config'

// Access values
CONFIG.gameVersion      // "0.0.1"
CONFIG.isDevelopment    // true
CONFIG.baseAttackSpeed  // 1000
CONFIG.features.achievements // false
```

## Current Variables

| Variable | Default | Type |
|----------|---------|------|
| `VITE_APP_TITLE` | Idle RPG | string |
| `VITE_GAME_VERSION` | 0.0.1 | string |
| `VITE_DEV_MODE` | true | boolean |
| `VITE_DEBUG_PHYSICS` | false | boolean |
| `VITE_SHOW_FPS` | false | boolean |
| `VITE_BASE_ATTACK_SPEED` | 1000 | number |
| `VITE_AUTO_SAVE_INTERVAL` | 1000 | number |
| `VITE_FEATURE_MULTIPLAYER` | false | boolean |
| `VITE_FEATURE_ACHIEVEMENTS` | false | boolean |

## Common Tasks

### Change a value
1. Edit `.env`
2. Save file
3. Restart `npm run dev`

### Add new variable
1. Add to `.env.example` (with description)
2. Add to `.env` (your value)
3. Add to `src/config.js` (with default)
4. Restart dev server

### Check current config
```javascript
// In browser console or code:
import CONFIG from './config'
console.log(CONFIG)
```

## Important Notes
- ⚠️ Must have `VITE_` prefix
- ⚠️ Restart dev server after changes
- ⚠️ All values are PUBLIC (in client bundle)
- ⚠️ Never commit secrets/API keys

## Files Created
- ✅ `.env` - Your local config (gitignored)
- ✅ `.env.example` - Template (committed)
- ✅ `src/config.js` - Config module (committed)
- ✅ `ENVIRONMENT_SETUP.md` - Full guide
- ✅ `SETUP_COMPLETE.md` - Setup summary

## Need Help?
Read `ENVIRONMENT_SETUP.md` for the complete guide!
