╔══════════════════════════════════════════════════════════════════╗
║           ENVIRONMENT VARIABLES QUICK REFERENCE                  ║
╚══════════════════════════════════════════════════════════════════╝

📁 FILES:
  .env.example      → Template (committed to git)
  .env.local        → Your local config (gitignored) ✓ EDIT THIS
  .env.production   → Production config (committed to git)

⚡ VITE RULES:
  • Only VITE_ prefixed variables are accessible in browser code
  • Restart dev server after changing .env files
  • Variables are replaced at build time (not runtime)

🎮 AVAILABLE VARIABLES:

Application:
  VITE_APP_TITLE              → App title
  VITE_APP_VERSION            → Version number

Development:
  VITE_ENABLE_DEBUG=true      → Enable debug mode
  VITE_ENABLE_LOGGING=true    → Enable console logs

Game:
  VITE_GAME_MODE=normal       → Game difficulty
  VITE_STARTING_GOLD=0        → Starting gold (testing)
  VITE_STARTING_LEVEL=1       → Starting level (testing)

Save System:
  VITE_ENABLE_AUTO_SAVE=true  → Enable auto-save
  VITE_SAVE_INTERVAL=1000     → Save every N milliseconds

💻 USAGE IN CODE:

  import { APP_CONFIG, GAME_CONFIG, log, debug } from './config'
  
  // Use configuration
  console.log(APP_CONFIG.title)
  log('This logs if VITE_ENABLE_LOGGING=true')
  debug('This logs if VITE_ENABLE_DEBUG=true')
  
  // Direct access
  const title = import.meta.env.VITE_APP_TITLE
  const isDev = import.meta.env.DEV
  const isProd = import.meta.env.PROD

🚀 COMMANDS:

  npm run dev      → Uses .env.local (development)
  npm run build    → Uses .env.production
  npm run preview  → Preview production build

📚 DOCS:

  ENVIRONMENT_SETUP.md         → Complete setup guide
  ENVIRONMENT_USAGE_EXAMPLES.md → Code examples
  src/config.js                → Centralized config module

⚠️  SECURITY:

  ❌ Never put secrets in VITE_ variables (visible in browser!)
  ✅ .env.local is gitignored (safe for local secrets)
  ✅ Use backend API for sensitive operations

═══════════════════════════════════════════════════════════════════

Need help? Check ENVIRONMENT_SETUP.md for detailed documentation.
